import styled from "styled-components";

export const BookmarkArea = styled.div`
  position:absolute;
  right:10px;
  top:10px;
  font-size:0.8rem;
  background:none;
  
  border:none;
  button{
    background:transparent;
    border:none;
    color:lightcoral;
    font-size:1.2rem;
    cursor:pointer;
  }
`
